[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
format = "bash -c 'pipenv run isort . && pipenv run black . && pipenv run flake8'"

[packages]

[dev-packages]
black = "*"
isort = "*"
flake8 = "*"

[requires]
python_version = "3.8"
